<div class="container form">
  <h1><%= t('views.goods.new.title') %></h1>
  <%= render 'form', good: @good, sub_kinds: @sub_kinds, kinds: @kinds, categories: @categories, departments: @departments, situations: @situations %>
</div>
